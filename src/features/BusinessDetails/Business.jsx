import { useQuery } from 'react-query';
import BusinessDetails from './BusinessDetails';
import './business-details.scss';

const Business = () => {
	// const { data } = useQuery('directories', fetchDirectories);

	return (
		<BusinessDetails /* image={image} description={description} name={name} */
		/>
	);
};

export default Business;
